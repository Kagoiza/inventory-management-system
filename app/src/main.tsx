import React from "react";
import ReactDOM from "react-dom/client";
import App from './components/App';  
import './components/auditor.css';

ReactDOM.createRoot(document.getElementById("root")!).render(
  <App />
  //<React.StrictMode>
  //<App />
  //</React.StrictMode>
);
